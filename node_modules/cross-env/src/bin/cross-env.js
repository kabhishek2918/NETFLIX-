import React from 'react';
  
  const Cross-env = () =>  {
	return (
	  <div>
	  </div>
	);
  }
  
  export default Cross-env;
  import React from 'react';
  
  const Cross-env = () =>  {
	return (
	  <div>
	  </div>
	);
  }
  
  export default Cross-env;
  #!/usr/bin/env node

const crossEnv = require('..')

crossEnv(process.argv.slice(2))
