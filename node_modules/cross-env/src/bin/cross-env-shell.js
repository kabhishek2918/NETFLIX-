import React from 'react';
  
  const Cross-env-shell = () =>  {
	return (
	  <div>
	  </div>
	);
  }
  
  export default Cross-env-shell;
  #!/usr/bin/env node

const crossEnv = require('..')

crossEnv(process.argv.slice(2), {shell: true})
